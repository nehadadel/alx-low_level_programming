#ifndef LISTS_H
#define LISTS_h

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

struct node {
int data;
struct node *next;
}Node;


int _putchar(char c);
size_t print_list(const list_t *h);

#endif
